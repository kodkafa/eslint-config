import type { Config } from "prettier";

export const config: Config = {
  arrowParens: "always",
  printWidth: 100,
  singleQuote: true,
  jsxSingleQuote: true,
  trailingComma: "all",
};

export default config;
